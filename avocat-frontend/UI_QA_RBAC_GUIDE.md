# UI QA RBAC Guide

Use `/dashboard/tools/qa-rbac` to:
- Switch mock user (admin/editor/viewer)
- Inspect current roles and permission count
- Check CRUD matrix per module
- Open quick links to verify button gating and forbidden state
