# Admin RBAC UI Guide

Routes:
- `/dashboard/admin/users`
- `/dashboard/admin/roles`
- `/dashboard/admin/permissions`

Features:
- Users CRUD + role assignment.
- Roles CRUD + permissions editor grouped by module with select-all.
- Permissions read-only grouped list + search.
