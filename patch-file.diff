 (cd "$(git rev-parse --show-toplevel)" && git apply --3way <<'EOF' 
diff --git a/docs/PROJECT_STRACTURE.md b/docs/PROJECT_STRACTURE.md
deleted file mode 100644
index 9b0407ec8272a82378e6445ec3d3f4063370d416..0000000000000000000000000000000000000000
--- a/docs/PROJECT_STRACTURE.md
+++ /dev/null
@@ -1,112 +0,0 @@
-وثيقة بناء الهيكلية الرئيسية لمشروع إدارة مكاتب المحاماة الذكي
-مقدمة
-
-تعد هذه الوثيقة جزءًا أساسيًا من خطة تنفيذ تطبيق لإدارة مكاتب المحاماة الذكي. الهدف من هذا النظام هو تحسين الكفاءة وتقليل الوقت الضائع في إدارة القضايا والعملاء والمستندات والفواتير من خلال دمج واجهة المستخدم مع البنية الخلفية بشكل فعال وآمن.
-
-1. الهيكلية الرئيسية للتطبيق
-1.1. الواجهة الأمامية (Frontend)
-
-الواجهة الأمامية هي الجزء الذي يتفاعل معه المستخدم النهائي. تم تصميم الواجهة باستخدام React لضمان استجابة سريعة وتجربة مستخدم سلسة.
-
-الصفحة الرئيسية:
-
-عرض الخدمات المقدمة من المكتب.
-
-روابط للمحتوى الأساسي (حول الشركة، الخدمات، اتصل بنا).
-
-إدارة العملاء (Client Management):
-
-واجهة لإضافة بيانات العملاء.
-
-عرض ومتابعة القضايا الخاصة بكل عميل.
-
-إمكانية رفع المستندات القانونية.
-
-إدارة القضايا (Case Management):
-
-عرض تفاصيل القضايا المختلفة ومتابعة مراحلها.
-
-تخصيص مواعيد المحاكم وتنبيه المحاميين والموظفين.
-
-الجدولة (Scheduling):
-
-تكامل مع أدوات التقويم (مثل Google Calendar) لجدولة المواعيد والمهام.
-
-بوابة العملاء (Client Portal):
-
-إنشاء صفحة مخصصة لكل عميل لعرض القضايا، المستندات، الفواتير، والتحديثات.
-
-الفوترة والمدفوعات (Billing & Payments):
-
-صفحة مخصصة لمراجعة الفواتير المدفوعة والمعلقة.
-
-دمج مع أنظمة الدفع الإلكتروني مثل Stripe أو PayPal.
-
-1.2. الواجهة الخلفية (Backend)
-
-الـ Backend مسؤول عن إدارة البيانات وتقديم الخدمات عبر API. يتم بناء هذه الواجهة باستخدام Laravel لتوفير أمان عالي وكفاءة في التعامل مع البيانات.
-
-إدارة الحسابات والصلاحيات (Authentication & Permissions):
-
-توفير نظام تسجيل دخول مع Sanctum وتحديد صلاحيات الوصول للمستخدمين (محامون، موظفون، عملاء).
-
-إدارة البيانات:
-
-إدارة القضايا: تنظيم بيانات القضايا وتفاصيلها.
-
-إدارة العملاء: تخزين بيانات العملاء، معلوماتهم الشخصية، وتاريخ التفاعل مع المكتب.
-
-الفواتير والمدفوعات (Billing & Payments):
-
-إنشاء الفواتير استنادًا إلى الوقت المُسجل.
-
-دمج مع أنظمة الدفع الإلكتروني لتسديد المدفوعات.
-
-إدارة المستندات (Document Management):
-
-تخزين وتنظيم المستندات القانونية المرتبطة بالقضايا مع توفر إمكانيات رفع المستندات وحمايتها.
-
-التقارير والتحليلات (Reporting & Analytics):
-
-توليد تقارير مالية وإدارية تشمل الإيرادات، المدفوعات، والفواتير.
-
-1.3. التكامل بين الـ Frontend و Backend
-
-يتم التواصل بين الواجهة الأمامية والخلفية باستخدام RESTful API لتبادل البيانات بشكل سلس وآمن.
-
-API: تتيح الواجهة الأمامية استعلامات مثل الحصول على بيانات القضايا، العملاء، والفواتير من الواجهة الخلفية.
-
-2. الدمج مع الموقع الإلكتروني
-2.1. تصميم الموقع
-
-الموقع الإلكتروني سيكون بمثابة واجهة عامة لشركة المحاماة ويوفر المعلومات الأساسية.
-
-صفحة رئيسية: عرض خدمات المكتب، المحاميين، وأحدث القضايا.
-
-صفحة الخدمات: تفاصيل حول الخدمات القانونية التي يقدمها المكتب.
-
-الصفحة الخاصة بالعملاء: رابط مباشر إلى بوابة العميل لتمكينه من الوصول إلى تفاصيل قضيته، المستندات، الفواتير، والرسائل.
-
-2.2. التكامل بين الموقع والتطبيق
-
-الموقع سيكون مرتبطًا مباشرة بتطبيق إدارة المكتب، حيث يمكن للعميل تسجيل الدخول إلى بوابته من خلال الموقع لمتابعة القضايا والمدفوعات.
-
-3. الأمان و الحماية
-
-نظرًا لحساسية البيانات القانونية، سيتضمن النظام تشغيل التوثيق المتعدد العوامل (MFA)، تشفير البيانات (AES-256)، وتطبيق سجلات التدقيق (Audit Logs) لضمان الأمان وحماية البيانات.
-
-4. التكامل مع الأدوات الخارجية
-
-لتوسيع إمكانيات النظام، سيتم دمجه مع الأدوات التالية:
-
-التخزين السحابي: مثل Google Drive أو Dropbox لتخزين المستندات القانونية.
-
-أدوات المحاسبة: مثل QuickBooks أو Xero لإدارة الفواتير والحسابات.
-
-التقويم: تكامل مع Google Calendar أو Microsoft Outlook لتزامن المواعيد.
-
-5. الخطوات المستقبلية
-
-إدخال الذكاء الاصطناعي (AI): لتحسين العمل من خلال أدوات مثل تلخيص المستندات أو أتمتة الفواتير.
-
-إضافة تكاملات جديدة: مع أدوات تحليل البيانات أو خدمات جديدة لتحسين كفاءة النظام.
\ No newline at end of file
diff --git a/docs/README.md b/docs/README.md
new file mode 100644
index 0000000000000000000000000000000000000000..20a2dd6a44801b1e3ab1f704b9f7ce6c028c2088
--- /dev/null
+++ b/docs/README.md
@@ -0,0 +1,17 @@
+# دليل التوثيق (docs)
+
+تم تنظيم ملفات التوثيق في هذا المجلد بحيث تغطي الصورة الكلية للنظام ثم تتفرع إلى أجزاء متخصصة.
+
+## الفهرس
+
+1. [الهيكلية العامة للتطبيق](./architecture-overview-ar.md)
+2. [هيكلية الباكند](./backend-architecture-ar.md)
+3. [هيكلية الفرونت إند](./frontend-architecture-ar.md)
+4. [هيكلية الموقع العام](./website-architecture-ar.md)
+5. [هيكلية واجهة المستخدم (UI)](./user-interface-architecture-ar.md)
+
+## ملاحظات تنظيمية
+
+- كل ملف يجيب على سؤال محدد لتقليل التداخل بين التوثيقات.
+- أسماء الملفات موحدة بصيغة واضحة: `<domain>-architecture-ar.md`.
+- أي تحديث معماري جديد يجب أن يبدأ من ملف النظرة العامة، ثم ينعكس على الملفات المتخصصة.
diff --git a/docs/architecture-overview-ar.md b/docs/architecture-overview-ar.md
new file mode 100644
index 0000000000000000000000000000000000000000..832609c3de33f201253ad097bb93688bdd8b2051
--- /dev/null
+++ b/docs/architecture-overview-ar.md
@@ -0,0 +1,38 @@
+# الهيكلية العامة للتطبيق
+
+هذا الملف يقدم تصورًا عالي المستوى لمنصة إدارة مكاتب المحاماة، وكيف تتكامل الأجزاء الأساسية مع بعضها.
+
+## 1) مكونات النظام الأساسية
+
+- **Backend API (Laravel):** مسؤول عن قواعد العمل، الصلاحيات، البيانات، والتكاملات.
+- **Frontend App (React):** لوحة التحكم والتطبيق الداخلي للمحامين والموظفين.
+- **Public Website:** واجهة تسويقية وتعريفية وبوابة دخول العملاء.
+- **User Interface Layer:** نظام المكونات المرئية وتجربة المستخدم المشتركة.
+
+## 2) تدفق العمل العام
+
+1. المستخدم يسجل الدخول عبر واجهة التطبيق أو البوابة.
+2. الطلبات تنتقل إلى الـ API مع التحقق من الهوية والصلاحيات.
+3. الـ Backend ينفذ قواعد العمل ويعيد استجابة معيارية.
+4. الواجهة تعرض البيانات عبر مكونات UI موحدة.
+
+## 3) المجالات الوظيفية الرئيسية
+
+- إدارة القضايا والجلسات والإجراءات.
+- إدارة العملاء والمستندات.
+- الفوترة والمدفوعات والتقارير.
+- الإشعارات وسجل الأنشطة.
+
+## 4) مبادئ معمارية معتمدة
+
+- فصل واضح بين طبقة العرض وطبقة الأعمال.
+- قابلية التوسع عبر وحدات مستقلة (Modular Domains).
+- اعتماد API contracts ثابتة بين الباكند والفرونت إند.
+- توحيد المصطلحات بين الفرق (قضية، جلسة، إجراء، فاتورة...).
+
+## 5) الروابط التفصيلية
+
+- تفاصيل الباكند: [backend-architecture-ar.md](./backend-architecture-ar.md)
+- تفاصيل الفرونت إند: [frontend-architecture-ar.md](./frontend-architecture-ar.md)
+- تفاصيل الموقع: [website-architecture-ar.md](./website-architecture-ar.md)
+- تفاصيل واجهة المستخدم: [user-interface-architecture-ar.md](./user-interface-architecture-ar.md)
diff --git a/docs/backend-architecture-ar.md b/docs/backend-architecture-ar.md
new file mode 100644
index 0000000000000000000000000000000000000000..8b97f13e94c4ad57252cd16e82e94a56f3259d6f
--- /dev/null
+++ b/docs/backend-architecture-ar.md
@@ -0,0 +1,48 @@
+# هيكلية الباكند
+
+يوثق هذا الملف بنية Backend المقترحة/الحالية لنظام إدارة مكاتب المحاماة.
+
+## 1) المسؤوليات الأساسية
+
+- إدارة الهوية والتفويض (AuthN/AuthZ).
+- تنفيذ قواعد الأعمال القانونية والمالية.
+- إدارة البيانات والعلاقات والعمليات الحساسة.
+- توفير API واضحة ومستقرة للفرونت إند والموقع.
+
+## 2) تقسيم الطبقات
+
+- **Routes / API Layer:** تعريف المسارات وإصدار النسخ (v1, v2).
+- **Controllers:** استقبال الطلبات وتوجيهها لطبقة الخدمة.
+- **Actions/Services:** تطبيق قواعد العمل الفعلية.
+- **Models/Repositories:** التعامل مع قاعدة البيانات.
+- **Resources/Transformers:** توحيد شكل الاستجابة.
+- **Jobs/Events/Listeners:** المعالجة غير المتزامنة والتكاملات.
+
+## 3) المجالات (Domains)
+
+- **Cases Domain:** القضايا، أنواعها، حالاتها، وربطها بالعملاء.
+- **Sessions Domain:** الجلسات، المواعيد، المحاضر، النتائج.
+- **Procedures Domain:** الإجراءات القانونية وسيرها الزمني.
+- **Clients Domain:** بيانات العملاء، المستندات، وسجل التواصل.
+- **Finance Domain:** الفواتير، المدفوعات، القيود، التقارير.
+- **Identity Domain:** المستخدمون، الأدوار، الصلاحيات.
+
+## 4) متطلبات الأمان
+
+- استخدام Sanctum (أو Passport عند الحاجة).
+- سياسات وصول دقيقة لكل مورد (Policies).
+- تتبع العمليات الحساسة عبر Audit Logs.
+- تقييد المعدل (Rate Limiting) لنقاط الدخول الحرجة.
+
+## 5) قواعد الجودة
+
+- توحيد الاستجابات والأخطاء (Response Envelope).
+- اختبارات Feature للمسارات الأساسية والتفويض.
+- Migrations آمنة وقابلة للتراجع.
+- فهارس DB للبحث والفلترة عالية الاستخدام.
+
+## 6) تكامل الباكند مع بقية النظام
+
+- الفرونت إند يعتمد على API contracts ثابتة.
+- الموقع العام يستهلك APIs مخصصة للعرض العام وبوابة العميل.
+- الإشعارات والتكاملات الخارجية تنفذ عبر Jobs منفصلة.
diff --git a/docs/backend-main-architecture-ar.md b/docs/backend-main-architecture-ar.md
deleted file mode 100644
index 3bab920d2d58c0cb88bef76352f011dd7e81aea1..0000000000000000000000000000000000000000
--- a/docs/backend-main-architecture-ar.md
+++ /dev/null
@@ -1,210 +0,0 @@
-
- خطة بناء الهيكلية الرئيسية للتطبيق (Backend أولًا)
-1. المبادئ الحاكمة للتطوير
-
-Domain-first: تنظيم الكود وفق نطاقات العمل (مثل: قضايا، عملاء، جلسات، مالية) لتجنب التداخل والتكرار.
-
-API-first: تصميم الواجهات البرمجية (API) أولًا، قبل ربطها بالواجهة الأمامية.
-
-Secure-by-default: جعل الأمان جزءًا من كل وحدة عمل، من المصادقة إلى التفويض والتدقيق.
-
-Event-driven حيث يلزم: استخدام Queue/Jobs للتعامل مع المهام غير المتزامنة مثل الإشعارات ومعالجة المستندات.
-
-Observability: التأكد من أن كل العمليات الحيوية ملاحظة عبر السجلات (logs) مع تتبع للأخطاء.
-
-2. الطبقات المعمارية المقترحة
-2.1 طبقة العرض (HTTP/API Layer)
-
-routes/api.php: ربط المسارات المناسبة على النسخة API (/api/v1/...).
-
-Controllers: مسؤول عن التعامل مع طلبات API:
-
-FormRequest: للتحقق من صحة المدخلات.
-
-Services: استدعاء خدمات النطاق، وتطبيق القواعد.
-
-Resource: العودة باستجابة موحدة لواجهة المستخدم.
-
-2.2 طبقة التطبيق (Application Layer)
-
-Actions/UseCases: إدارة سيناريوهات العمل مثل:
-
-CreateCaseAction: لإنشاء قضية جديدة.
-
-AssignLawyerToCaseAction: لتعيين محامي للقضية.
-
-RecordInvoicePaymentAction: لتسجيل المدفوعات الخاصة بالفواتير.
-
-2.3 طبقة النطاق (Domain Layer)
-
-Entities: مثل Case, Client, Invoice.
-
-Domain Services: توفير القواعد المعقدة مثل حساب الفواتير أو معالجة القضايا.
-
-Policies: لضمان أحقية المستخدم في تنفيذ العمليات.
-
-2.4 طبقة البنية التحتية (Infrastructure Layer)
-
-Eloquent Models + Migrations: تنظيم النماذج مع الجداول والعمليات ذات الصلة.
-
-Queue/Jobs/Events: إدارة العمليات الثقيلة غير المتزامنة مثل الإشعارات والتكاملات.
-
-Integrations: التكامل مع خدمات خارجية مثل البريد الإلكتروني وSMS.
-
-Cache: تحسين الأداء باستخدام التخزين المؤقت.
-
-Rate Limiting: تقييد الوصول لتقليل الضغط على الواجهات البرمجية.
-
-3. التقسيم المعياري (Module Boundaries)
-3.1 Auth & Identity
-
-تسجيل دخول/خروج: إصدار رموز المصادقة عبر Laravel Sanctum.
-
-إدارة الجلسات والأدوار: باستخدام RBAC.
-
-دعم الـ MFA: إمكانية إضافة مصادقة متعددة العوامل مستقبلًا.
-
-3.2 Cases (القضايا)
-
-إدارة القضايا: إنشاء القضايا وتعيين المحامين، ومرحلة سير القضايا (Open → In Progress → Closed).
-
-تخزين السجل الزمني: كل حدث يخص القضية سيكون مسجلاً في قاعدة البيانات.
-
-3.3 Sessions & Procedures
-
-إدارة الجلسات القانونية: حفظ مواعيد الجلسات ونتائجها.
-
-إدارة الإجراءات: ربط الإجراءات بالمحامي أو الموظف المسؤول.
-
-3.4 Documents
-
-رفع المستندات: تخزينها على الخوادم أو عبر سحابة مثل S3.
-
-تصفية البيانات: إدارة الميتاداتا مثل نوع الملف، المصدر، وربطها بالقضايا.
-
-3.5 Finance (المالية)
-
-الفواتير والمدفوعات: إنشاء الفواتير وجداول الدفع.
-
-تسوية المدفوعات: إدارة الإيرادات والنفقات، والتحقق من الدفعات.
-
-3.6 Notifications (الإشعارات)
-
-إشعارات عبر قنوات متعددة: البريد الإلكتروني، الرسائل النصية.
-
-إشعارات مستندة إلى الأحداث: مثل إشعار قريب لموعد الجلسة.
-
-3.7 Search & Reporting (البحث والتقارير)
-
-استعلامات تقارير: مثل البحث في القضايا النشطة، مواعيد الجلسات.
-
-تقارير تشغيلية: تقارير تخص القضايا الجارية والمدفوعات.
-
-4. هيكل المجلدات المقترح (Laravel)
-app/
-  Domain/
-    Cases/
-    Finance/
-    Documents/
-    Notifications/
-  Application/
-    Cases/Actions/
-    Finance/Actions/
-  Http/
-    Controllers/Api/V1/
-    Requests/
-    Resources/
-  Policies/
-  Services/Integrations/
-  Jobs/
-  Events/
-  Listeners/
-5. دورة البيانات (Data Lifecycle)
-
-العميل يرسل طلب API.
-
-يتم التحقق من المدخلات عبر FormRequest.
-
-Controller يستدعي Action المناسب.
-
-Action يطبق القواعد ويجري العمليات داخل قاعدة البيانات.
-
-إطلاق Event عند إتمام العملية بنجاح.
-
-Resource يعيد استجابة موحدة للـ Frontend.
-
-العمليات الثقيلة تُدار باستخدام Job.
-
-6. قاعدة البيانات: قواعد تأسيسية
-
-استخدام UUID/ULID للكيانات الهامة.
-
-إضافة فهارس للحقل الأكثر استخدامًا في البحث (مثل case_number، client_id).
-
-Soft Deletes للكيانات الهامة (حذف غير دائم).
-
-جداول Pivot واضحة للعلاقات المتعددة.
-
-7. الأمان والحوكمة
-
-Laravel Sanctum أو Passport لإدارة المصادقة.
-
-RBAC عبر roles, permissions.
-
-سياسات الوصول (Policy) لكل مورد رئيسي.
-
-تدقيق العمليات الحساسة: مثل تعديل حالة القضية أو تسوية المدفوعات.
-
-Rate Limiting على النقاط الحساسة.
-
-8. استراتيجية التنفيذ المرحلي (Backend First Roadmap)
-المرحلة 1: Foundation
-
-إعداد Auth + Users + Roles + Permissions.
-
-توحيد Error handling وAPI response envelope.
-
-تجهيز Logging/Audit.
-
-المرحلة 2: Core Legal Flow
-
-تطوير CRUD لـ Cases, Sessions, Procedures.
-
-ربط الأحداث الأساسية بإشعارات داخلية.
-
-كتابة اختبارات Feature أساسية.
-
-المرحلة 3: Documents + Finance
-
-إنشاء أنظمة رفع المستندات والفواتير.
-
-تطوير Jobs للعمليات غير المتزامنة مثل الإشعارات.
-
-المرحلة 4: Integrations + Hardening
-
-تحسين التكامل مع الأدوات الخارجية (Email, SMS).
-
-تحسين الأداء عبر Indexing، Caching، query optimization.
-
-اختبار E2E.
-
-9. تعريف Done (لكل وحدة Backend)
-
-التوثيق الجيد لـ API contracts.
-
-اختبارات Feature أساسية.
-
-أمان مُفعّل.
-
-سجلات ومقاييس مراقبة كافية.
-
-10. ما الذي يستلمه فريق الـ Frontend بعد انتهاء كل مرحلة؟
-
-تحديث ملف Swagger/OpenAPI.
-
-أمثلة Responses معيارية.
-
-قائمة صلاحيات endpoint-by-endpoint.
-
-بيئة staging مستقرة ببيانات seed.
-
diff --git a/docs/frontend-architecture-ar.md b/docs/frontend-architecture-ar.md
new file mode 100644
index 0000000000000000000000000000000000000000..f72dc2b75154cf71d45092417f03743e87f9ba71
--- /dev/null
+++ b/docs/frontend-architecture-ar.md
@@ -0,0 +1,36 @@
+# هيكلية الفرونت إند (تطبيق الإدارة)
+
+يركز هذا الملف على تطبيق الواجهة الأمامية الداخلي المستخدم من قبل فريق المكتب.
+
+## 1) أهداف الفرونت إند
+
+- تقديم تجربة استخدام سريعة وواضحة للأعمال اليومية.
+- فصل الصفحات والميزات حسب المجالات القانونية.
+- تقليل التكرار عبر مكونات مشتركة قابلة لإعادة الاستخدام.
+
+## 2) تقسيم بنيوي مقترح
+
+- **app/**: الإعداد العام، الراوتنج، المزودات (Providers).
+- **features/**: وحدات العمل حسب المجال (cases, clients, finance...).
+- **shared/**: المكونات العامة، الأدوات، الخدمات، الثيم.
+- **locales/**: ملفات الترجمة والرسائل.
+
+## 3) إدارة الحالة والبيانات
+
+- طبقة API client موحدة للطلبات.
+- إدارة حالة محلية للواجهات التفاعلية.
+- كاش للبيانات المتكررة لتقليل الضغط على الباكند.
+- التعامل الموحد مع الأخطاء ورسائل التنبيه.
+
+## 4) مبادئ الواجهة
+
+- تصميم موجه للمهام (Task-oriented UI).
+- دعم اللغة العربية واتجاه RTL بشكل أصيل.
+- قابلية الوصول الأساسية (لوحة مفاتيح، تباين، Labels).
+- مكونات موحدة للنماذج والجداول والفلاتر.
+
+## 5) التكامل مع الباكند
+
+- الاعتماد على عقود API موثقة.
+- عزل DTO/Transformers في طرف الواجهة عند الحاجة.
+- توحيد سيناريوهات المصادقة وتحديث الجلسة.
diff --git a/docs/frontend-architecture-report-ar.md b/docs/frontend-architecture-report-ar.md
deleted file mode 100644
index fbcc7f3d67d2499854b14a18f2d5662c0165b670..0000000000000000000000000000000000000000
--- a/docs/frontend-architecture-report-ar.md
+++ /dev/null
@@ -1,142 +0,0 @@
- 
-# تقرير دقيق للبنية الأمامية (Frontend) — Avocat
-
-## 1) ملخص تنفيذي
-الواجهة الأمامية في مشروع **Avocat** مبنية على **React + Vite**، مع توجيه عبر `react-router-dom`، وإدارة حالة هجينة بين **Context PI** و **Redux Toolkit**، مع تنظيم قائم على الوحدات (Features) لتسهيل التوسع والصيانة. نقطة الدخول تقوم بتركيب مزودي السياق لرئيسيين (اللغة، التنبيهات، المصادقة، الأمن) قبل تحميل التطبيق.  
-
-البنية الحالية مناسبة للبدء فوراً بتطوير الواجهة الأمامية، لأنها تحتوي بالفعل على:
-- قشرة تطبيق واضحة (App Shell + Dashboard).
-- نظام توجيه محمي بالصلاحيات.
-- طبقة خدمات API موحدة.
-- تنظيم ملفات واضح يفصل بين `features` و `shared`.
-
----
-
-## 2) نقطة الدخول وسلسلة التهيئة
-**نقطة الدخول:** `src/main.tsx`
-
-عند الإقلاع، يتم إنشاء `createBrowserRouter` بمسار جذري، ثم لف التطبيق بعدة Providers بالترتيب التالي:
-1. `LanguageProvider`
-2. `AlertProvider`
-3. Redux `Provider`
-4. `AuthProvider`
-5. `SecurityProvider`
-6. `RouterProvider` داخل `Suspense`
-
-هذا التسلسل يعطي أفضلية للوظائف المشتركة (اللغة/التنبيه/المصادقة/الأمن) قبل تحميل الشاشات، ويضمن أن الحراسة الأمنية تعمل مبكراً.
-
----
-
-## 3) طبقة التوجيه (Routing) وحماية الوصول
-### 3.1 المستوى الأعلى — `src/app/App.tsx`
-التطبيق يقسم المسارات الأساسية إلى:
-- `/` صفحة رئيسية.
-- `/login` و`/signup` مع Redirect إذا كان المستخدم مسجلاً.
-- `/dashboard/*` مسارات داخلية محمية عبر `RequireAuth`.
-- مسار fallback يعيد إلى الصفحة الرئيسية.
-
-كما يتم تغليف المسارات الداخلية بمزودي الثيم، السايدبار، والـ Spinner.
-
-### 3.2 مسارات لوحة التحكم — `src/app/routes/AuthRoutes.jsx`
-داخل `/dashboard/*` يتم تحميل الشاشات بطريقة **Lazy Loading** لتقليل حجم الحزمة الابتدائية وتحسين الأداء.  
-كل مسار حساس محاط بـ `PermissionGuard` عبر مكوّن `Guarded`، ويرتبط بخريطة صلاحيات مركزية `permissionMap`.
-
-أمثلة وحدات فعالة ضمن المسارات:
-- العملاء وخدمة العملاء.
-- القضايا والخدمات القانونية.
-- التقارير.
-- المالية.
-- الإعدادات.
-- الإدارة (المستخدمون/الأدوار/الصلاحيات).
-
-النهج المستخدم حالياً ممتاز لتطبيق يعتمد RBAC لأنه يربط **التوجيه + الصلاحية + اسم الوحدة** في مكان واحد.
-
----
-
-## 4) إدارة الحالة (State Management)
-### 4.1 الحالة العالمية الخفيفة عبر Context
-داخل `src/shared/contexts` توجد سياقات مسؤولة عن:
-- المصادقة `AuthContext`
-- اللغة `LanguageContext`
-- الثيم `ThemeContext`
-- التنبيهات `AlertContext`
-- الشريط الجانبي `SidebarContext`
-- مؤشر التحميل `SpinnerContext`
-
-هذا مناسب للحالات العرضية والعابرة بين الشاشات.
-
-### 4.2 Redux Toolkit لحالات المجالات
-يوجد Store في `src/app/store/store.js` مع Slice خاص بالعملاء `clientsSlice.js` يتضمن:
-- بيانات العملاء.
-- loading/error.
-- async actions لجلب وتحديث الحالة عبر API.
-
-الخلاصة: المشروع يستخدم **نمطاً هجيناً عملياً**: Context للحالات العامة + Redux للبيانات الأكثر تعقيداً وقابلية للتوسع.
-
----
-
-## 5) طبقة التكامل مع الـ API
-طبقة الاتصال الخلفي موجودة في:
-`src/shared/services/api/*`
-
-وتتضمن ملفات متخصصة لكل نطاق (عملاء، قضايا، تقارير، محاكم، مصاريف، جلسات...).  
-هذا يفصل منطق الشبكة عن الواجهات ويسهّل:
-- إعادة الاستخدام.
-- الاختبار.
-- توحيد إعداد Axios عبر `axiosConfig.js`.
-
----
-
-## 6) الهيكلية المجلدية المعتمدة
-الهيكلية الفعلية تدعم تقسيم واضح:
-- `src/app`: التطبيق الأساسي، التوجيه، المتجر.
-- `src/features`: الوحدات الوظيفية (قضايا، تقويم، تقارير، مالية...).
-- `src/shared`: المكونات المشتركة، الخدمات، السياقات، الحماية، الأنواع.
-- `src/styles` + `src/shared/styles`: طبقات التنسيق والـ tokens.
-- `public`: أصول ثابتة وأيقونات وخطوط.
-
-هذه البنية مناسبة جداً لفرق متعددة، لأن كل فريق يمكنه امتلاك Feature دون كسر الأجزاء الأخرى.
-
----
-
-## 7) البنية الأمنية في الواجهة
-يوجد مستوى أمني واضح على الواجهة عبر:
-- `SecurityProvider`.
-- `PermissionGuard`.
-- خرائط صلاحيات مركزية (مثل `permissionMap`).
-
-مهم: الحماية في الواجهة هي **تجربة استخدام** ومنع عرض عناصر غير مصرح بها، لكن يجب دائماً أن تتكامل مع تحكم نهائي من الـ Backend وهو النمط الصحيح).
-
----
-
-## 8) البنية التقنية والبناء (Build)
-من `vite.config.js`:
-- دعم PWA عبر `vite-plugin-pwa`.
-- ضغط ملفات الإنتاج (`vite-plugin-compression`).
-- تقسيم حزم vendor.
-- aliases منظمة (`@app`, `@features`, `@shared`...).
-
-هذا يقلل زمن التطوير ويجعل الاستيراد أوضح ويجهز المشروع للتوسع.
-
----
-
-## 9) تقييم الجاهزية للبدء بالواجهة الأمامية
-**نعم، يمكن البدء مباشرة بالـ Frontend** لأن الركائز الأساسية جاهزة:
-- توجيه محمي.
-- هيكلية Features.
-- خدمات API.
-- سياقات مشتركة.
-- إعدادات بناء حديثة.
-
-### أولويات تنفيذ مقترحة (Sprint 0)
-1. توحيد معايير كتابة المكونات (TS/JS، naming، lint rules).
-2. اعتماد نمط موحد لجلب البيانات (React Query أو الاستمرار بالنهج الحالي مع توثيق موحد).
-3. توحيد تصميم صفحات القوائم/التفاصيل عبر Components مشتركة.
-4. إكمال توثيق خريطة الصلاحيات UI ↔ API.
-5. إضافة اختبارات smoke لمسارات dashboard الأساسية.
-
----
-
-## 10) خاتمة
-البنية الأمامية الحالية ليست مجرد بداية مبدئية، بل **أساس قوي للإنتاج** إذا تم ضبط التوحيد والاختبارات مبكراً.  
-الهيكل الحالي يسهل نمو التطبيق إلى نظام قانوني متكامل دون إعادة هيكلة جذرية لاحقاً.
diff --git a/docs/frontend=website.md b/docs/frontend=website.md
deleted file mode 100644
index 29c62e19f9b5a9cd8dc317457944a9de201a1ace..0000000000000000000000000000000000000000
--- a/docs/frontend=website.md
+++ /dev/null
@@ -1,278 +0,0 @@
- 
-+# الهيكلية الرئيسية للتطبيق (Backend أولًا)
-
-> هذا المستند يقدّم تصورًا عمليًا ومفصلًا لبناء الواجهة الخلفية كنواة للنظام، بحيث تكون إدارة البيانات، الصلاحيات، والتكاملات اهزة قبل التوسع في الواجهة الأمامية.
-
-## 1) المبادئ الحاكمة
-
-- **Domain-first**: تنظيم الكود حول نطاقات العمل (Cases, Clients, Sessions, Finance...) بدل التنظيم حسب نوع الملف فقط.
-- **API-first**: كل سلوك عمل يجب أن يكون متاحًا عبر API ثابتة قبل ربطه بالـ Frontend.
-- **Secure-by-default**: المصادقة، التفويض، والتدقيق (Audit) ليست إضافات، بل جزء من كل وحدة.
-- **Event-driven حيث يلزم**: المهام الثقيلة وغير المتزامنة (إشعارات، معالجة مستندات، تكاملات) تُدار عبر Queue/Jobs.
-- **Observability**: تسجيل الأحداث المهمة، قياس الأداء، وتتبّع الأخطاء منذ البداية.
-
-## 2) الطبقات المعمارية المقترحة
-
-### 2.1 طبقة العرض (HTTP/API Layer)
-- `routes/api.php`: تعريف المسارات وفق نسخ API (`/api/v1/...`).
-- `Controllers`: مسؤولة فقط عن:
-  - التحقق الشكلي عبر `FormRequest`.
-  - استدعاء خدمات النطاق.
-  - إعادة `Resource` موحد للاستجابة.
-
-### 2.2 طبقة التطبيق (Application Layer)
-- `Actions / UseCases`: تمثل سيناريوهات واضحة مثل:
-  - `CreateCaseAction`
-  - `AssignLawyerToCaseAction`
-  - `RecordInvoicePaymentAction`
-- كل Action ينسق بين النماذج والخدمات ويطبق قواعد العمل.
-
-### 2.3 طبقة النطاق (Domain Layer)
-- كيانات النطاق: القضايا، الجلسات، الإجراءات، الفواتير، التحصيل.
-- خدمات نطاق (Domain Services): القواعد المعقدة العابرة للكيانات.
-- سياسات النطاق (Policies): التحقق من حق المستخدم في تنفيذ عملية معينة.
-
-### 2.4 طبقة البنية التحتية (Infrastructure Layer)
-- Eloquent Models + Migrations.
-- Queue/Jobs/Events.
-- Integrations (Email/SMS/Calendar/Webhooks/Storage).
-- Cache + Rate limiting + Logging.
-
-## 3) التقسيم المعياري (Module Boundaries)
-
-### 3.1 Auth & Identity
-- تسجيل دخول/خروج، إصدار Token، إدارة الجلسات.
-- إدارة المستخدمين والأدوار والصلاحيات (RBAC).
-- دعم MFA مستقبلًا ضمن نفس الوحدة.
-
-### 3.2 Cases (القضايا)
-- إنشاء القضية وربطها بالعميل/المحكمة/المحامي.
-- إدارة الحالة (Workflow state): جديد → جارٍ → مؤجل → مغلق.
-- سجل زمني موحد لكل الأحداث المتعلقة بالقضية.
-
-### 3.3 Sessions & Procedures
-- إدارة الجلسات القانونية وتواريخها ونتائجها.
-- إدارة الإجراءات (Procedures) وربطها بالقضية والموظف المسؤول.
-- ربط التذكيرات والتنبيهات بالمواعيد.
-
-### 3.4 Documents
-- رفع ملفات آمن (Local/S3-compatible).
-- Metadata موحدة: نوع الملف، المصدر، الارتباط بالقضية/العميل.
-- مسار مراجعة/اعتماد للمستندات الحساسة (اختياري مرحليًا).
-
-### 3.5 Finance
-- الفواتير، المدفوعات، الإيرادات، المصروفات.
-- قواعد محاسبية واضحة (حالات الفاتورة + تسوية المدفوعات).
-- تجهيز endpoints للتقارير المالية لاحقًا.
-
-### 3.6 Notifications
-- قنوات متعددة: داخل النظام + Email (ثم SMS/WhatsApp).
-- رسائل مبنية على Events (مثال: قرب موعد جلسة).
-- قابلية إعادة المحاولة والفشل المراقب عبر Queue.
-
-### 3.7 Search & Reporting
-- بحث سريع عبر جداول مرجعية أساسية.
-- Endpoints تقارير تشغيلية (قضايا نشطة، جلسات اليوم، تحصيلات).
-- فصل استعلامات التقارير الثقيلة عن CRUD الأساسي.
-
-## 4) هيكل المجلدات المقترح (Laravel)
-
-```text
-app/
-  Domain/
-    Cases/
-    Finance/
-    Documents/
-    Notifications/
-  Application/
-    Cases/Actions/
-    Finance/Actions/
-  Http/
-    Controllers/Api/V1/
-    Requests/
-    Resources/
-  Policies/
-  Services/Integrations/
-  Jobs/
-  Events/
-  Listeners/
-```
-
-> الهدف من هذا الهيكل هو تقليل تداخل المسؤوليات، وتسهيل الاختبار، وتمكين فرق متعددة من العمل المتوازي.
-
-## 5) دورة البيانات (Data Lifecycle)
-
-1. العميل يرسل طلب API.
-2. `FormRequest` يتحقق من البنية والقيود الأساسية.
-3. `Controller` يستدعي `Action` مناسب.
-4. `Action` يطبق قواعد النطاق ويجري عمليات DB داخل Transaction عند الحاجة.
-5. يتم إطلاق Event عند نجاح العملية.
-6. `Resource` يرجع استجابة موحدة للـ Frontend.
-7. `Job` غير متزامن يتكفل بالإشعارات/التكاملات الثقيلة.
-
-## 6) قاعدة البيانات: قواعد تأسيسية
-
-- اعتماد **UUID/ULID** للكيانات الأساسية المعرضة خارجيًا.
-- فهارس إلزامية على الحقول الأكثر استخدامًا في البحث (`case_number`, `client_id`, `session_date`, ...).
-- أعمدة تدقيق موحدة: `created_by`, `updated_by` حيث تنطبق.
-- Soft Deletes للكيانات ذات الأثر القانوني.
-- جداول Pivot واضحة للعلاقات متعددة-لمتعددة مع قيود فريدة (Unique constraints).
-
-## 7) الأمان والحوكمة
-
-- Laravel Sanctum (أو Passport عند الحاجة لـ OAuth كامل).
-- RBAC عبر `roles`, `permissions`, و`role_user`.
-- سياسات وصول دقيقة (Policy لكل مورد رئيسي).
-- تدقيق العمليات الحساسة: تعديل حالة قضية، حذف مستند، تسوية مالية.
-- Rate limiting على endpoints الحساسة (Auth, Search, Export).
-
-## 8) استراتيجية التنفيذ المرحلي (Backend First Roadmap)
-
-### المرحلة 1: Foundation
-- إعداد Auth + Users + Roles + Permissions.
-- توحيد Error handling وAPI response envelope.
-- تجهيز Logging/Audit baseline.
-
-### المرحلة 2: Core Legal Flow
-- Cases + Sessions + Procedures (CRUD + Policies + Filters).
-- ربط الأحداث الأساسية بإشعارات داخلية.
-- كتابة اختبارات Feature حرجة.
-
-### المرحلة 3: Documents + Finance
-- رفع/تنظيم المستندات.
-- الفواتير والمدفوعات والتقارير الأولية.
-- Jobs للعمليات غير المتزامنة.
-
-### المرحلة 4: Integrations + Hardening
-- Email/SMS/Webhooks.
-- تحسين الأداء (Indexes, Caching, query tuning).
-- سيناريوهات E2E وتثبيت مقاييس SLO.
-
-## 9) تعريف Done (لكل وحدة Backend)
-
-تُعتبر الوحدة "جاهزة" عندما تحقق:
-
-- API contracts موثقة وواضحة.
-- تغطية اختبار Feature للمسارات الأساسية + حالات التفويض.
-- سياسات أمان مفعّلة.
-- Observability كافية (Logs + metrics للأخطاء المهمة).
-- Migration آمنة وقابلة للتراجع.
-
-## 10) ما الذي يستلمه فريق الـ Frontend بعد انتهاء كل مرحلة؟
-
-- ملف OpenAPI/Swagger محدث.
-- أمثلة Responses معيارية.
-- قائمة صلاحيات endpoint-by-endpoint.
-- بيئة staging مستقرة ببيانات seed واقعية.
-
----
-
-بهذا الترتيب، تبدأ المنصة من نقطة قوة: **بيانات متماسكة + قواعد عمل محكمة + APIs مستقرة**، مما يقلل إعادة العمل عند بناء الواجهة الأمامية ويزيد جودة المنتج النهائي.
- بناءً على تقرير تحليل الصفحة الرئيسية (Landing Page) لموقعك، إليك خطة التطوير لتحديث وتحسين الهيكلية:
-
-1. أهداف التطوير:
-
-تحسين التنظيم الهيكلي: نقل المحتوى النصي من الصفحة إلى ملفات locales لمزيد من القابلية للتوسّع.
-
-التخصيص حسب حالة المستخدم: تحسين الانتقال بين حالة "زائر" و"مستخدم مسجّل" عبر تحسين واجهات المصادقة.
-
-التفاعل المتقدم: إضافة المزيد من الحركات التفاعلية (animations) والمحتوى المتغير بناءً على بيانات المستخدم.
-
-2. المكونات الأساسية للتطوير:
-2.1. تعديل الهيكل العام للـ Landing Page
-
-Hero Section (واجهة المقدمة):
-
-تحسين استخدام motionVariants في framer-motion لإضافة تأثيرات ديناميكية أكبر على النصوص والصور.
-
-استخدام صورة خلفية متميزة يتم تحميلها من fichier assets بشكل مركزي.
-
-إضافة CTA رئيسية/ثانوية تتغير بناءً على حالة المصادقة.
-
-Services Section (الخدمات الأساسية):
-
-استخدام Cards لتوضيح الخدمة مع تأثيرات تحريك إضافية عند التفاعل (hover).
-
-إمكانية إضافة إحصائيات ديناميكية تُحدّث مباشرةً من الـ Backend (مثل عدد القضايا المعالجة).
-
-Why Avocat Section (لماذا أفوكات):
-
-تحسين العرض البصري لبطاقات القيمة الثلاث مع إضافة نصوص مؤثرة تتعلق بالأمان والخدمات المتميزة.
-
-CTA Section (دعوة إجراء نهائية):
-
-تحسين الزر الذهبي لإظهار تأثيرات CSS مع gradient-gold لتحسين التجربة البصرية.
-
-Trust Metrics Section (مؤشرات الثقة):
-
-تحديث المؤشرات بحيث يتم جلبها ديناميكيًا من Backend.
-
-Footer (التذييل):
-
-إضافة روابط لصفحات حول المكتب، السياسة القانونية، شروط الاستخدام.
-
-2.2. تخصيص المحتوى للزوار والمستخدمين المسجلين:
-
-النصوص:
-
-نقل النصوص الثابتة (مثل النصوص التعريفية) إلى ملفات locales لسهولة الترجمة والصيانة.
-
-استخدام نظام i18n (ترجمة النصوص) لتمكين التبديل السلس بين اللغات.
-
-الأزرار والتوجيه:
-
-تفعيل الأزرار وفقًا لحالة المصادقة: زر "تسجيل الدخول" و"إنشاء حساب" للزوار، وزر مباشر لـ "/dashboard" للمستخدمين المسجلين.
-
-2.3. تحسين تجربة المستخدم (UX)
-
-التفاعل المتقدم:
-
-إضافة تأثيرات حركية (motion) لتحسين التفاعل في الأقسام مثل Hero و Services Section باستخدام framer-motion.
-
-إضافة تأثيرات scale/opacity للصورة الرئيسية لتحسين التجربة التفاعلية.
-
-التفاعل مع النصوص والصور:
-
-الصور: استخدام Lazy Load لتحسين تحميل الصور التي تظهر في الـ Hero والـ Services section.
-
-تحسين الانتقال بين الصفحات: استخدام react-router-dom بنمط ديناميكي للانتقال بين صفحات الدخول، التسجيل، واللوحة الرئيسية (Dashboard).
-
-2.4. تحسين الأمان والمرونة في الواجهة
-
-التبديل بين الأنماط (Themes): ضمان أن ThemeToggle يعمل بشكل سلس بين الوضع الفاتح والداكن.
-
-اللغة (Language): تحسين واجهة التبديل بين اللغات العربية والإنجليزية باستخدام LanguageContext لضمان تجربة مستخدم مريحة (RTL و LTR).
-
-2.5. ملاحظات التحسينات المستقبلية:
-
-إدارة الأصول (Assets):
-
-توحيد إدارة الصور والرموز (مثل الـ LandingPageImg و BackgroundSvg) داخل فهرس أصول موحد.
-
-إضافة متغيرات جديدة:
-
-الاستفادة من copy.hero.highlight في تطوير النصوص التعريفية المتميزة في الـ Hero.
-
-تحسين الاتصال بين الـ Frontend و Backend:
-
-استخدام React Context لتحسين إدارة حالة المصادقة بشكل مركزي عبر الصفحات.
-
-3. خطوات التنفيذ:
-
-التهيئة الأولية: تنظيم النصوص داخل ملفات locales للمحتوى المتعدد اللغات.
-
-التطوير في الـ Frontend: تحسين هيكل الصفحة عبر تعديل الـ HomePage.jsx لإضافة الحركات المتقدمة والتفاعل بناءً على الحالة.
-
-التطوير في الـ Backend: إضافة API لجلب البيانات الإحصائية والديناميكية مثل Trust Metrics.
-
-اختبار التفاعل بين الـ Frontend و Backend: التأكد من سلامة البيانات المدخلات والمعالجة في النظام.
-
-تحسين الأمان: إضافة طبقات أمان جديدة للصفحات مثل MFA و التشفير.
-
-خلاصة:
-
-المشروع سيتطلب توحيد النصوص داخل ملفات locales لضمان قابلية التوسّع.
-
-سيكون هناك تحسينات إضافية على الحركات التفاعلية و التخصيص حسب حالة المستخدم لضمان تجربة مستخدم متكاملة وسلسة.
-
-إضافة تحسينات أمنية، مثل MFA وحماية البيانات، مما يعزز التفاعل والثقة في النظام.
\ No newline at end of file
diff --git a/docs/user-interface-architecture-ar.md b/docs/user-interface-architecture-ar.md
new file mode 100644
index 0000000000000000000000000000000000000000..28e642545a7e8ab2a554447c00057df6bf1726d4
--- /dev/null
+++ b/docs/user-interface-architecture-ar.md
@@ -0,0 +1,37 @@
+# هيكلية واجهة المستخدم (UI)
+
+يركز هذا الملف على النظام المرئي (Design System) وتجربة المستخدم المشتركة بين التطبيق والموقع.
+
+## 1) مبادئ التصميم
+
+- الوضوح أولًا: واجهات بسيطة ومباشرة.
+- الاتساق: نفس الأنماط لنفس السلوك.
+- الأولوية للمعلومة القانونية المهمة.
+- تقليل خطوات تنفيذ المهام المتكررة.
+
+## 2) طبقات المكونات
+
+- **Foundations:** الألوان، الخطوط، المسافات، الظلال.
+- **Primitives:** أزرار، حقول إدخال، شارات، رسائل تنبيه.
+- **Composites:** جداول، نماذج، بطاقات، نوافذ منبثقة.
+- **Patterns:** صفحات التفاصيل، القوائم، لوحات المتابعة.
+
+## 3) إرشادات تجربة المستخدم
+
+- دعم RTL كامل.
+- تغذية راجعة فورية بعد كل إجراء (نجاح/خطأ/تحميل).
+- حالات واضحة عند غياب البيانات (Empty States).
+- رسائل خطأ مفهومة وقابلة للتنفيذ.
+
+## 4) قابلية الوصول
+
+- تباين ألوان مناسب.
+- دعم التنقل بلوحة المفاتيح.
+- تسميات واضحة للحقول والأزرار.
+- اتساق ترتيب العناصر في النماذج.
+
+## 5) الحوكمة والصيانة
+
+- توثيق أي مكون جديد قبل اعتماده.
+- منع إنشاء مكونات مكررة دون حاجة.
+- مراجعة دورية للأنماط غير المستخدمة.
diff --git a/docs/website-architecture-ar.md b/docs/website-architecture-ar.md
new file mode 100644
index 0000000000000000000000000000000000000000..888f24de7aa1895ca458ebc9fa555c1cbc268705
--- /dev/null
+++ b/docs/website-architecture-ar.md
@@ -0,0 +1,35 @@
+# هيكلية الموقع العام (Website)
+
+هذا الملف يشرح بنية الموقع التعريفي/التسويقي المرتبط بالتطبيق.
+
+## 1) دور الموقع
+
+- عرض خدمات المكتب بشكل احترافي.
+- بناء الثقة عبر مؤشرات وإنجازات واضحة.
+- تحويل الزوار إلى عملاء أو مستخدمين مسجلين.
+
+## 2) الصفحات الأساسية
+
+- الصفحة الرئيسية (Landing Page).
+- الخدمات.
+- من نحن.
+- تواصل معنا.
+- تسجيل الدخول/بوابة العميل.
+
+## 3) المحتوى والتدويل
+
+- فصل النصوص في ملفات ترجمة (locales).
+- دعم ثنائي اللغة (عربي/إنجليزي) مع قابلية التوسع.
+- إدارة CTA حسب حالة المستخدم (زائر/عميل مسجل).
+
+## 4) التكامل مع النظام
+
+- ربط بوابة العميل بنظام المصادقة المركزي.
+- استهلاك APIs عامة لعرض الإحصائيات والمحتوى الديناميكي.
+- الحفاظ على تجربة انتقال سلسة من الموقع إلى التطبيق.
+
+## 5) مؤشرات الجودة
+
+- سرعة تحميل جيدة للصفحات الأساسية.
+- بنية SEO صحيحة للعناوين والوصف.
+- تصميم متجاوب لجميع المقاسات.
 
EOF
)